name: Disabled Next.js Pages Deploy
on: [workflow_dispatch]
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled because the project is hosted on Vercel."
